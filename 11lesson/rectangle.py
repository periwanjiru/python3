class Rectangle():
    def area (self, base, height):
        int_base = int (base)
        int_height = int (height)
        area = int_base * int_height
        return area
base = input("what is the base of the rectangle?:")
height = input("what is the height of the rectangle?:")
rectangle = Rectangle()
area = rectangle.area (base,height)
print(area)
class Rectangle():
    def area,perimeter (self, base,height):
        int_base = int (base)
        int_height = int (height)
        int_width = int (width)
        int_length = int (length)
        area = int base * int height, perimeter = (width+lenth) * 2 
print(area, perimeter)

        
        